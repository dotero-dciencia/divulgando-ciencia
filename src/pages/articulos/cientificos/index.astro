---
import Articulos from "@/layouts/Articulos.astro";
const articulos = (await Astro.glob('./*.md')).sort((a, b) => Date.parse(b.frontmatter.fecha) - Date.parse(a.frontmatter.fecha));
---
<Articulos articulos={articulos} title="Artículos" inicial="c"/>