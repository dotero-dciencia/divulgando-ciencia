---
import Layout from "@/layouts/fr/Layout.astro";
import Menu from "@/components/fr/Menu.astro";
const {title, description, image, frontmatter, robots} = Astro.props
---

<Layout {title} {description} {image} {frontmatter} {robots}>
    <Menu design="articles"></Menu>
    <slot></slot>
</Layout>